
@{
    ViewBag.Title = "Instrument";
}

<h2>Instrument</h2>
<a class="btn btn-primary btn-lg" href=@Url.Action("New", "Rentals")>New</a>
<a style="margin-left: 15px;" class="btn btn-primary btn-lg" href=@Url.Action("Used", "Rentals")>Used</a>
<div>
    <img src=@Html.Raw((String)ViewBag.Logo) />
    @Html.Raw((String)ViewBag.Output)
</div>